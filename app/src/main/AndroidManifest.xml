<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shachengxxx.jenkinsdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="TEST_TYPE" android:value="test"/>

        <meta-data
            android:name="APP_ID"
            android:value="${APP_ID}\0" />
        <!--AF采集 配置-->
        <meta-data android:name="CHANNEL" android:value="${CHANNEL}" />

        <!-- 谷歌登录-->
        <meta-data
            android:name="GOOGLE_SERVER_CLIENT_ID"
            android:value="ads${GOOGLE_SERVER_CLIENT_ID}"/>

        <!--facebook 登录配置-->
<!--        <meta-data-->
<!--            android:name="com.facebook.sdk.ApplicationId"-->
<!--            android:value="${FACEBOOKID}" />-->
<!--        <activity-->
<!--            android:name="com.facebook.CustomTabActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.VIEW" />-->
<!--                <category android:name="android.intent.category.DEFAULT" />-->
<!--                <category android:name="android.intent.category.BROWSABLE" />-->
<!--                <data android:scheme="fb${FACEBOOKID}" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
        <!--facebook 登录配置-->
    </application>

</manifest>